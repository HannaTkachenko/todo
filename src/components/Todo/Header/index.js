import React from 'react';
import styles from './../../../styles/styles.module.scss'

const Header = () => {
  return (
    <header className={styles.header}>
      <h1>Hanna's To-do list</h1>
    </header>
  );
}

export default Header;
